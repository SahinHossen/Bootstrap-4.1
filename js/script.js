// Popover JS
$(function () {
  $('[data-toggle="popover"]').popover()
})
// Popover JS

// Popover Dismiss JS
$('.popover-dismiss').popover({
  trigger: 'focus'
})
// Popover Dismiss JS

// Tooltip JS
$(function () {
  $('[data-toggle="tooltip"]').tooltip()
})
// Tooltip JS
